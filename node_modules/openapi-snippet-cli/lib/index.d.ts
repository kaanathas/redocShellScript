import { Command, flags } from '@oclif/command';
import { OpenAPI } from 'openapi-types';
declare class OpenapiSnippetCli extends Command {
    static description: string;
    static flags: {
        version: import("@oclif/parser/lib/flags").IBooleanFlag<void>;
        help: import("@oclif/parser/lib/flags").IBooleanFlag<void>;
        targets: flags.IOptionFlag<string[]>;
        ext: flags.IOptionFlag<string>;
        output: flags.IOptionFlag<string>;
    };
    static args: {
        name: string;
        description: string;
    }[];
    run(): Promise<void>;
    withSnippets(api: OpenAPI.Document, targets: ReadonlyArray<string>): OpenAPI.Document;
    fetchSnippets(api: OpenAPI.Document, path: string, method: string, targets: ReadonlyArray<string>): {
        lang: string;
        source: string;
    }[];
}
export = OpenapiSnippetCli;
